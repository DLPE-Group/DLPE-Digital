# Documentation site toolchain. Install with:
#   pip install -r requirements-docs.txt
# Then:  python3 -m mkdocs serve   (preview)  /  python3 -m mkdocs build  (render ./site)
mkdocs>=1.6
mkdocs-material>=9.5
